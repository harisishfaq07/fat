- edit ||= false
- collapsed = session[:lead_presence].nil? # && @lead.errors.empty?
= subtitle :lead_presence, collapsed, t(:web_presence)
.section
  %small#lead_presence_intro{ hidden_if(!collapsed) }
    = t(:intro, t(:web_presence_small)) unless edit
  #lead_presence{ hidden_if(collapsed) }
    %table
      %tr
        %td
          .label.top #{t :blog}:
          = f.text_field :blog , placeholder: "Blog URL..."
        %td= spacer
        %td
          .label.top #{t :twitter}:
          = f.text_field :twitter , placeholder: "Profile URL..."
      %tr
        %td
          .label #{t :linked_in}:
          = f.text_field :linkedin , placeholder: "Profile URL..."
        %td= spacer
        %td
          .label #{t :facebook}:
          = f.text_field :facebook , placeholder: "Profile URL..."
      %tr
        %td
          .label #{t :skype}:
          = f.text_field :skype , placeholder: "Profile URL..."
        %td= spacer
        %td= spacer

